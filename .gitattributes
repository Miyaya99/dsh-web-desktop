# Normalize line endings: LF in the repository, native on checkout.
* text=auto eol=lf

# Windows launcher scripts must keep CRLF so PowerShell 5.1 parses them reliably.
*.ps1 text eol=crlf

# Binary assets - never touch.
*.ico binary
*.png binary
